Summarize the architecture of: $ARGUMENTS

May be one module or the whole system. Read the relevant code first, then explain how it works while
making every load-bearing claim checkable:
- Cite file:line for each component, and quote the real function/class signature rather than paraphrasing.
- Show the data flow (what produces what; where the DAG cache and provenance sit).
- State explicitly, in a separate "unverified" list, anything you could NOT confirm from the code. Do
  not smooth over gaps.

Keep it tight: an orientation I can verify by jumping to the cited lines, not prose I have to trust.
