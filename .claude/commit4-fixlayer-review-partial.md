# Commit 4 fix-layer review - PARTIAL results extracted from the workflow journal

Run ID `wf_bffe18aa-e96`. Extracted mid-run as insurance against a session limit.
Dimension reports returned: 5/5. Refuter verdicts returned: 28.

## dimension report (v2:10605a5cfa0a0e8b5a30b86c7993b86d33092e21cc592a0d10fd579fdb0a10bb)
- clean: False
- summary: The substantive version claim is CORRECT: `scipy.stats.chatterjeexi` first shipped in scipy 1.15.0 (the 1.15.0 release notes list it under "scipy.stats improvements"; the versioned API page exists at scipy-1.15.0 with the exact signature the test uses and 404s at scipy-1.14.1), the floor is not too low (the only other >=1.15 API in the tree, `permutation_test(rng=...)` in src/quebra/analyzers/permutation.py:136, also arrived in 1.15.0), and scipy 1.15.0 declares requires-python >=3.10 and ships cp311 wheels so the CI 3.11 leg is safe; `pytest tests/test_r_cross_implementation.py -q` passes 23. What is NOT clean is completeness: three "1.17" sites survive the fix, one of them the bolded lead sentence of the very requirement being corrected.
  - **IMPORTANT** `spec/spectests06.md:1041` - R6.8 still opens with the bolded sentence "The `scipy` floor is raised to 1.17", so the requirement now asserts both 1.17 and 1.15 seven lines apart; the fix layer added a correcting paragraph instead of correcting the paragraph.
    - evidence: grep -n "raised to 1.17" spec/spectests06.md -> 1041:**The `scipy` floor is raised to 1.17, and the reason in the earlier draft was wrong.** That draft ... and sed -n 1048p -> **The floor is 1.15, and an earlier draft of this requirement said 1.17.**  git diff shows line 1041 untouched by the fix layer.
    - fix: Rewrite line 1041 to "The `scipy` floor is raised to 1.15, and the reason in the earlier draft was wrong." and fold the two paragraphs into one; while there, break the run-on seam at line 1053 where "The reason string is corrected to match. Combined with R6.0.10 - ..." is glued mid-line.
  - **IMPORTANT** `tests/test_r_cross_implementation.py:255` - Only one of the two "1.17" sites in the test file was fixed: the docstring first line still reads "(scipy >= 1.17)", thirteen lines above the corrected reason string at :268.
    - evidence: grep -n "1.17" tests/test_r_cross_implementation.py -> 255:    """`scipy.stats.chatterjeexi` (scipy >= 1.17) - independent, Python, no R needed.  (git diff of that file shows exactly one changed line, :268.)
    - fix: Change line 255 to "(scipy >= 1.15)". This is the CLAUDE.md §4 rule "fix every site of a class, not the one that failed", and R6.8 acceptance 5 already justifies touching this file for precisely this reason.
  - **MINOR** `spec/spectests06.md:1185` - The phase budget table still names R6.8 as "scipy floor raised to 1.17" and still gives its file count as 4, contradicting the acceptance clause the fix layer raised to 6 files.
    - evidence: sed -n 1185p spec/spectests06.md -> | R6.8 coverage reported in CI, scipy floor raised to 1.17 | +0 | 4 | 1 |   versus spec/spectests06.md:1064 "Files: **6** (`pyproject.toml`, `Makefile`, `CONTRIBUTING.md`, `.github/workflows/ci.yml`, `tests/test_r_cross_implementation.py` ... and this spec ...)".
    - fix: Update the row to "scipy floor raised to 1.15" with Files 6. The same table's R6.10 row still reads 2 against the acceptance clause's new 4, so fix both rows in one pass - the budget table is the STOP mechanism in CLAUDE.md §9 and is only useful if it matches the acceptance clauses.
  - **MINOR** `tests/test_r_cross_implementation.py:267` - The corrected reason string is attached to a guard that can never fire for the reason it states: `pytest.importorskip("scipy.stats")` skips only when scipy.stats is unimportable, which cannot happen since scipy is a hard runtime dependency, so the version message is unreachable and the real version guard is the `hasattr` at :270.
    - evidence: sed -n 267,271p tests/test_r_cross_implementation.py shows importorskip("scipy.stats", reason="scipy.stats.chatterjeexi needs scipy >= 1.15") followed by `if not hasattr(scipy_xi, "chatterjeexi")` with its own skip message. pyproject.toml:31 declares scipy as a required dependency.
    - fix: Either move the version sentence onto the :270 hasattr skip message (which is the guard that would actually fire below 1.15) or drop the importorskip in favour of a plain import. Note this also confirms acceptance 4's claim that the guards at :270 and :286 are dead at >=1.15 - chatterjeexi is present in every scipy from 1.15.0 onward - so recording them as dead is correct.

## dimension report (v2:6817bf43987ab7987446df2b8625ab393f023df233ea8c618e885eea64e8e542)
- clean: False
- summary: Fix-layer items 3 and 4 are CORRECT: both rewritten citations resolve to real tests that genuinely assert the claimed property, and I confirmed by in-memory mutation that both tests are live (a 1e-6 perturbation of c1's sqrt(12), a 1e-4 perturbation of its numerator, and a 1e-3 perturbation of gamma all break test_eq16_reduces_to_eq4_for_a_single_segment; test_eq7_is_the_classical_anderson_darling compares c2._eq7 against an independently written textbook form with measured relative agreement 3e-16 to 1e-13). No better-matching test was passed over: test_the_laplace_statistic_matches_the_paper is an external-value oracle, not an eq(16)->eq(4) identity, and test_eq7_at_gamma_one_matches_the_limiting_ad_null is a distributional check, so neither fits the claims as written. Two MINOR gaps remain, both about how tightly the cited evidence actually binds.
  - **MINOR** `jobs/bench/arms.py:46-48` - The re-pointed citation pins a local re-implementation of the arm's construction, not the arm itself, and the cited test explicitly disclaims the exactness arms.py attributes to it.
    - evidence: arms.py:46-48 says "The exact exponential oracle survives: with `tau` fixed and exponential gaps, conditional on `N` the ratios `T_i/tau` are exactly uniform order statistics, which is what `tests/test_checks_statistics.py::test_eq7_at_gamma_one_matches_the_limiting_ad_null` pins." That test (tests/test_checks_statistics.py:99-127) uses its own local helper `exponential_segment` (tests/test_checks_statistics.py:56-61), never `arms.py`'s `_truncate_at`/`_tau_for` (jobs/bench/arms.py:204-226). `grep -rn "arm_a" tests/` returns nothing, so no test exercises the arm generator at all; a drift in `_tau_for` or a switch to an event-determined tau would leave the cited "pin" green. The test's own docstring (line 104) also states "the agreement below is excellent-at-this-N rather than exact" and calls itself a "Transcription pin for eq (7)", not a pin of the generator.
    - fix: Reword to what the test actually shows, e.g. "the same construction is exercised empirically by tests/test_checks_statistics.py::test_eq7_at_gamma_one_matches_the_limiting_ad_null, which checks the eq (7) null against Marsaglia's limiting distribution at n = 20 and 50 - it re-implements the construction rather than importing this module, so it is evidence about the property, not a guard on this generator."
  - **MINOR** `src/quebra/analyzers/checks/c2_anderson_darling.py:14-16` - The docstring's "agreement is to float precision" overstates what the cited test enforces: its tolerance is rel=1e-9, four to seven orders looser than the agreement actually observed.
    - evidence: tests/test_checks_statistics.py:82 asserts `c2._eq7(x, tau, 1.0) == pytest.approx(_classical_ad(x, tau), rel=1e-9)`. Measured actual relative agreement across the parametrised n (3, 5, 20, 60, 200): 3.4e-16, 2.8e-16, 4.3e-15, 7.7e-15, 9.9e-14. Injecting a uniform relative error into `_eq7` and re-running the same assertion: eps=1e-13 passes, 1e-11 passes, 1e-10 passes, 5e-10 passes, 1e-9 fails. So a genuine 5e-10 transcription discrepancy would ship with the docstring still claiming float-precision agreement.
    - fix: Either tighten the test to rel=1e-12 (comfortably above the 9.9e-14 worst observed case) so the assertion enforces the claim, or reword the docstring to "agreement is to 1e-9 relative, and is measured at 1e-16 to 1e-13".

## dimension report (v2:7f74215fd114b2164bae1bae1018828ff0d99e7703e72d268a63408d84940439)
- clean: False
- summary: I ran the guard (2 passed), independently re-measured its counts against the tree (38 citation sites / 18 distinct, and the per-prefix split src/ 13, jobs/ 14, docs/ 7, scripts/ 1, AGENTS.md 2, CONTRIBUTING.md 1 — all exactly as the file claims), and ran 10 mutants of the regex, _canonical, SCOPE and the unread check. Three mutations go red as intended (mistyped prefix, identity _canonical, prefix-only regex — the last caught by the newly broadened positive control), but four leave the suite GREEN, and the file's own docstring contains an invented citation of exactly the kind it exists to forbid.
  - **CRITICAL** `tests/test_stale_references.py:5` - The guard file's own docstring cites `tests/test_oracle_audit.py`, a module that has never existed in any commit and is mentioned nowhere else in the repository - the exact invented-locator defect the guard was written to catch, invisible to it because `tests/` is silently out of SCOPE.
    - evidence: `ls tests/ | grep -i oracle` -> no match (exit 1); `git log --all --oneline -- tests/test_oracle_audit.py | wc -l` -> 0; `grep -rn "oracle_audit" --include=*.py --include=*.md .` -> one hit only, tests/test_stale_references.py:5. Running the file's own _stale() over tests/ flags it: `STALE tests/test_stale_references.py:5 -> tests/test_oracle_audit.py`. Line 5 reads: "which is why it exists where `tests/test_oracle_audit.py` deliberately has no counterpart. `AGENTS.md` section 10 forbids inventing a locator".
    - fix: Either name the real artifact the sentence means (there is no oracle-audit test in tests/; the oracle rule lives in AGENTS.md section 7), or drop the path and write the point without a locator, e.g. "which is why it exists where an oracle audit deliberately has no automated counterpart".
  - **CRITICAL** `tests/test_stale_references.py:130-142` - The new per-prefix "unread" assertion cannot catch a DELETED SCOPE entry - it iterates over SCOPE itself - so the comment claiming it closes that hole is false, and the mutation it names explicitly (dropping `src/`, "where both of the real stale citations this guard was written for actually lived") leaves the suite GREEN.
    - evidence: Mutants built from the shipped file with only SCOPE edited, run with pytest: `test_mut_drop_src.py :: 2 passed`, `test_mut_drop_jobs.py :: 2 passed`, `test_mut_drop_docs.py :: 2 passed`, `test_mut_drop_scripts.py :: 2 passed`; only `test_mut_mistype_src.py` ("srcc/") goes red (1 failed). Dropping src/ takes the count 38 -> 25, still >= MIN_CITATIONS 20. Line 131 asserts the opposite: "Without this a SCOPE entry deleted or mistyped would silently stop the guard reading a whole tree, and the count floor would not notice"; line 69-70 repeats it: "The real guard on the walk is the per-prefix assertion below: a count floor cannot catch a TRUNCATED scope".
    - fix: Pin SCOPE itself, not just its readability: assert the literal tuple equals the expected set (or assert a per-prefix minimum count derived from the measured 13/14/7/1/2/1) so a removed entry fails. If that is not wanted, correct both comments to say the assertion catches a MISTYPED prefix only, and state that a deleted entry remains undetected.
  - **IMPORTANT** `tests/test_stale_references.py:33-34` - The fix-layer-introduced figure "`.claude/` ... carries three such citations, all in review ledgers that are rewritten on every review" does not reproduce under any reading: the measurement is 5 stale citation sites, 4 distinct never-existing modules, and only 1 of the 5 lives in a tracked file.
    - evidence: Applying the shipped CITATION regex and _canonical to .claude/ gives 5 stale sites: .claude/review-findings-0006-commit4.md:13 (tests/test_checks_c2.py), :31 (test_checks_c2.py), :32 (test_checks_c1.py), :75 (test_calendar_tau_truncation.py) - all four in an UNTRACKED file - plus .claude/review-findings-0008.md:163 (tests/test_carve_differential.py), the only tracked one. `git log --all --oneline -- <each>` returns 0 for all four distinct modules. So: 5 sites, 4 distinct, 1 tracked; none of these is 3.
    - fix: Recompute and state which quantity is being reported (sites vs distinct modules, tracked vs working tree), e.g. "one such citation in a tracked ledger (`.claude/review-findings-0008.md:163`) and four more in an untracked review ledger"; or drop the number and say the ledgers carry such citations without counting them.
  - **IMPORTANT** `tests/test_stale_references.py:1` - The opening rule "Every `tests/test_*.py` path cited from a tracked file names a file that exists" overstates what SCOPE covers: the docstring accounts for excluding `spec/` and `.claude/` but never mentions that `tests/` (22 citation sites) and `pyproject.toml` (2) are also unread, and tests/ currently holds a stale citation.
    - evidence: Citation sites in tracked files outside SCOPE, by top level: {'.claude': 387, 'CLAUDE.md': 2, 'pyproject.toml': 2, 'spec': 123, 'tests': 22}. Applying _stale to tests/ finds tests/test_marker_discipline.py:72 -> tests/test_foo.py (an illustrative path in a docstring) and the finding-1 citation at tests/test_stale_references.py:5. SCOPE at line 48 is ("src/", "jobs/", "docs/", "scripts/", "AGENTS.md", "CONTRIBUTING.md").
    - fix: Either narrow the opening sentence to the scope actually walked ("cited from a tracked file under src/, jobs/, docs/, scripts/, AGENTS.md or CONTRIBUTING.md"), or add `tests/` to SCOPE and handle the two placeholder paths - which would immediately fail on tests/test_marker_discipline.py:72 and on line 5 of this file, i.e. it would have caught finding 1.
  - **MINOR** `tests/test_stale_references.py:60-67` - The lookbehind the comment calls "load-bearing" has no test - removing it leaves the suite green - and its claim to admit "nothing else" is false, while relative-path citation forms are missed entirely in both directions.
    - evidence: `test_mut_nolookbehind.py :: 2 passed` (regex changed to `(?:tests/)?test_[a-z_0-9]+\.py`, suite still green, because no phantom-producing word is currently in scope). Probing the shipped regex: 'my-test_run.py' -> ['test_run.py'] (a phantom the lookbehind does not reject, contradicting line 65 "admits `test_x.py` and `tests/test_x.py` and nothing else"); './tests/test_km.py' -> [], '../tests/test_km.py' -> [], 'qre_tool/tests/test_km.py' -> [] (the `/` in the lookbehind kills both alternatives, so any relative or nested path form is unseen); 'tests/test_KM.py' -> [] and 'tests/test-km.py' -> []. A loose scan of all in-scope tracked files found no live instance of any missed form, so this is latent, not currently firing.
    - fix: Add a unit assertion over CITATION with a table of accept/reject strings (`tests/test_x.py`, `` `test_x.py` ``, `latest_run.py`, `contest_data.py`, `my-test_run.py`, `./tests/test_x.py`) so the lookbehind and the optional prefix each have a test that goes red when changed; and either allow a leading `./`/`../` in the pattern or record relative forms as an accepted blind spot.

## dimension report (v2:731241fc2e591cb046986b97bee631275915106e8f3e20789477db673dcd7bd0)
- clean: False
- summary: Item 6's core mechanism is CORRECT: `@` suppresses command echoing, not stdout, so `make cov 2>/dev/null | head -3` really does deliver "selector: not slow and not heavy and not r" and "data/real_private: present" into the pipe, and 196 lines land in the step summary (far under the 1 MiB cap), with the closing fence always reached. No `fail_under` exists anywhere (no `.coveragerc`, no `[tool.coverage]` table, no `--cov-fail-under`), so the comment's "never gated" claim is true, and every other claim in the comment block checks out - including the "earlier draft dropped require-selector" one, which is corroborated by `.claude/review-findings-0006-commit4.md:54`. Two defects remain: the tee pipe silently swallows `make cov`'s exit status under GitHub's default shell, and two numbers the fix layer newly wrote into spectests06.md around this step do not reproduce. On the "second run" question my recommendation is to KEEP the additive step: folding `--cov` into `make check` would make the gating command differ from the local `make check` (the stated invariant of the step above it) and would put the coverage tracer inside the gate, for a saving of roughly one bare-suite run per leg. The cost is real but small on a workflow with no `timeout-minutes`.
  - **IMPORTANT** `.github/workflows/ci.yml:67` - The `| tee` pipe discards `make cov`'s exit status under GitHub's default shell, so a `require-selector` failure (or any pytest failure) produces step exit 0, no continue-on-error warning annotation, and a job summary containing a header and an EMPTY fenced block with nothing saying why - the one failure mode the comment above the step names as its reason for calling the target.
    - evidence: The workflow sets no `shell:` and no `defaults:` (`grep -rn "defaults\|shell:" .github/workflows/` -> no match), so the default is `bash -e {0}`, without pipefail. Synthetic reproduction of the exact recipe shape in the scratchpad: `bash -e -c 'make cov | tee -a summary.txt; echo "step exit would be: $?"'` with a missing selector file printed `make: *** [require-selector] Error 1` to stderr and then `step exit would be: 0`; `cat summary.txt` was 0 lines. The same command under `bash -eo pipefail -c` exited 2. Note stderr is not piped either, so require-selector's two explanatory echoes (Makefile:20-22, both `>&2`) never reach the summary.
    - fix: In the run block: `set -o pipefail` is not enough on its own, because `set -e` would then skip the closing fence and leave an unterminated code block. Use `make cov 2>&1 | tee -a "$GITHUB_STEP_SUMMARY"; rc=${PIPESTATUS[0]}` then the closing fence, then `[ "$rc" -eq 0 ] || echo "make cov exited $rc" >> "$GITHUB_STEP_SUMMARY"`. That keeps the step non-gating (continue-on-error already guarantees that) while making a failed coverage run visible in the summary instead of an empty block.
  - **IMPORTANT** `spec/spectests06.md:999` - The sentence the fix layer added to document this step's own output says "587 passed and 2 skipped"; the run it cites gives 589 passed and 2 skipped, and the figure contradicts a second number the SAME fix layer added 191 lines later.
    - evidence: `make cov` on the shipped tree prints `TOTAL 6522 1961 70%` - matching the table immediately above line 999 exactly, so it is the same code state - followed by `589 passed, 2 skipped, 11 warnings in 69.19s`. `pytest --collect-only -q -m "not slow and not heavy and not r"` reports `591 tests collected`, i.e. 589 + 2. spec/spectests06.md:1190, also added by this fix layer, states "Measured on completion: 591" and line 1191-1194 derives "These sum to +56, so 535 becomes 591". 587 + 2 = 589, not 591. `git diff spec/spectests06.md` shows line 999 is a `+` line, so this is a number the fix layer introduced, not one it inherited.
    - fix: Change "587 passed and 2 skipped" to "589 passed and 2 skipped" at spec/spectests06.md:999, taken from the `make cov` run that produced the 6522/1961/70% row beside it.
  - **MINOR** `spec/spectests06.md:1033` - The cost figures the fix layer added for this CI step - a coverage run at 51.6 s against a bare run at 39.6 s, giving "roughly 2.6 minutes of runner time per push" - do not reproduce on this machine, where the covered run is about 38% slower than stated.
    - evidence: `/usr/bin/time -f "%e" make test` -> `589 passed, 2 skipped ... in 49.37s`, wall 51.53 s. `/usr/bin/time -f "%e" make cov` -> `589 passed, 2 skipped ... in 69.19s (0:01:09)`, wall 71.33 s. So the measured pair here is 51.5 s bare / 71.3 s covered (a 20 s delta), not 39.6 / 51.6 (a 12 s delta), and three legs is roughly 3.6 minutes rather than 2.6. The relative overhead is consistent (1.38x here vs 1.30x quoted), so this reads as a stale or differently-loaded measurement rather than an arithmetic error, and GitHub runners are slower than either.
    - fix: Either re-measure and state the pair with the machine it was taken on, or - since a runner's wall time is not this machine's - restate the cost as the ratio the spec can actually stand behind ("a covered run is roughly 1.3 to 1.4x a bare one, so the step is close to a second full suite per leg") and drop the absolute seconds.
  - **MINOR** `.github/workflows/ci.yml:54-56` - The comment records drafting history ("An earlier draft copied it and had already dropped `require-selector`") in a permanent file, which AGENTS.md section 10 forbids; the constraint is worth stating, the draft is not.
    - evidence: The claim itself is TRUE - `.claude/review-findings-0006-commit4.md:54` says "The copy already dropped `require-selector`" - but no such draft is in history (`git log -S"--cov" --oneline -- .github/workflows/ci.yml` returns nothing; `git show HEAD:.github/workflows/ci.yml | tail -20` ends at `make deps`), so no future reader can ever see the draft this sentence describes. AGENTS.md section 10: "A module docstring states what the module is for and what contract it holds, not how the author got there ... If a past bug still constrains the design, state the constraint."
    - fix: Replace the two clauses with the constraint alone: "Runs the TARGET rather than a copy of its command, because `cov` depends on `require-selector`, which is what stops an empty selector file running every excluded tier."

## dimension report (v2:72af080702a839e77fb2ae8491e59292a8f170e2cda8fb3b1a4198a9c6b17eda)
- clean: False
- summary: I re-derived every corrected figure in spec/spectests06.md independently. Most reproduce exactly: 38 citation sites / 18 distinct modules in R6.10's scope (and the per-prefix split src 13, jobs 14, docs 7, scripts 1, AGENTS.md 2, CONTRIBUTING.md 1), 38 sites with exactly 3 stale at HEAD, git rev-list over all commits confirming test_checks_c1.py, test_checks_c2.py and test_calendar_tau_truncation.py never existed, collect 591 (baseline 535 confirmed via git archive at 47169eb; deltas summing to +56; R6.5 +31, R6.6 +4, R6.7 +6 all confirmed by per-file collect at b695ef2 vs now), the coverage table (70%, 6522 statements, 1961 missed, kaplan_meier 79%, mtbf 80%, allan 22%, fidelity 22%, identity 100%, closure 94%, provenance 99%) reproduced by make cov, "Thirty-four Kaplan-Meier tests" = 30 + 4 properties, "587 passed and 2 skipped" at CHECKPOINT 6.8 (589 now, +2 from R6.10), and the acceptance-5 file counts 6 and 4 against git status. Four figures do not reproduce: the spec still asserts the 1.17 floor in two places, and the "five stale references under spec/" enumeration is both undercounted and mislocated.
  - **IMPORTANT** `spec/spectests06.md:1041` - The fix layer corrected the scipy floor to 1.15 in the R6.8 body and acceptance 4 but left two sites still asserting 1.17, one of them the bold normative statement of what R6.8 does, which contradicts the shipped pyproject.toml and its own correction seven lines below.
    - evidence: sed -n '1041p;1185p' spec/spectests06.md -> 1041: "**The `scipy` floor is raised to 1.17, and the reason in the earlier draft was wrong.**"; 1185: "| R6.8 coverage reported in CI, scipy floor raised to 1.17 | +0 | 4 | 1 |". Against grep -n scipy pyproject.toml -> 31: "scipy>=1.15", and spec 1048: "**The floor is 1.15, and an earlier draft of this requirement said 1.17.**"
    - fix: Rewrite line 1041 so it does not state 1.17 as the landed floor (drop the number or say 1.15), and change the budget-table row at 1185 to "scipy floor raised to 1.15".
  - **IMPORTANT** `spec/spectests06.md:1148` - The new "five stale references under spec/" enumeration undercounts and mislocates: under the guard's own shipped regex there are 9 stale citation sites on 7 lines under spec/, not five, and two of the three line numbers given (:1112, :1130) point at a horizontal rule and at a sentence about .claude/ ledgers, neither of which contains a citation.
    - evidence: Replicating the shipped CITATION regex plus _canonical over `git ls-files spec/` yields spectests06.md:241, :243 (x2), :1124, :1126 (x2), :1151 and specvalidity08.md:117, :158 = 9 sites on 7 lines. `awk 'NR==1112||NR==1130' spec/spectests06.md` prints "---" and "any case not a stable measurement, since most of those sites sit in `.claude/` review ledgers that". The figure five is exactly what the OLD prefix-required pattern gives (241, 1124, 1151, 117, 158) - the pattern this same fix layer broadened because it missed the bare form.
    - fix: Re-derive with the shipped pattern: seven stale lines / nine sites, five of them this spec's own at :241, :243, :1124, :1126 and :1151 (the :243 and :1126 pairs being the bare-form quotations), plus the two real ones at specvalidity08.md:117 and :158.
  - **MINOR** `tests/test_stale_references.py:33` - The guard docstring's ".claude/ carries three such citations" does not reproduce under any counting rule: tracked .claude/ carries one stale citation, and including the untracked review ledger it carries five.
    - evidence: Scanning .claude/ with the file's own CITATION regex and _canonical: tracked only -> `.claude/review-findings-0008.md:163 -> tests/test_carve_differential.py`, one site. Including untracked -> also `.claude/review-findings-0006-commit4.md:13, :31, :32, :75`, five sites total. Neither is three.
    - fix: State the measured figure and its basis, e.g. "tracked `.claude/` carries one such citation, in a review ledger rewritten on every review", or drop the count and keep the qualitative point.
  - **MINOR** `spec/spectests06.md:1033` - The new CI-cost figures (51.6 s additive, 39.6 s bare, a 12 s difference) do not reproduce on this checkout; the qualitative point that the step is additive stands, but the seconds are 25 to 90 percent off measurement.
    - evidence: `make cov` here: "589 passed, 2 skipped ... in 68.40s". Bare `python3 -m pytest -m "not slow and not heavy and not r" -q`: "589 passed, 2 skipped ... in 45.13s". Additive cost measures 68.4 s, not 51.6 s; the bare-versus-covered difference measures 23 s, not 12 s.
    - fix: Requote from a fresh run naming the machine, or state the cost as a ratio ("a covered run costs roughly 1.5x a bare one, and the step is a whole extra run per leg") so it does not go stale on the next machine.

## refuter verdicts
- [HOLDS  ] v2:b9961be16d54167d391abfe2eb46a9b498efb9e4a23e352244abdf2eec1895a6: Not refuted; all three cited commands reproduce exactly. (1) grep -n "raised to 1.17" spec/spectests06.md returns 1041 (the bolded "The `scipy` floor is raised to 1.17, and the reason in the earlier draft was wrong.") and 1185. (2) sed -n 1048p returns "**The floor is 1.15, and an earlier draft of this requirement said 1.17.**" So R6.8 asserts both numbers in the present tense seven lines apart. (
- [HOLDS  ] v2:584cfbacd9d1472e5f08fe6b0718cd99089b0e6522925b41cee7f15bdbf30b67: The finding holds, and is understated. Verified by command, not by reading.

CONFIRMED (1): spec/spectests06.md:1041 still reads "**The `scipy` floor is raised to 1.17, and the reason in the earlier draft was wrong.**" while :1048 reads "**The floor is 1.15, and an earlier draft of this requirement said 1.17.**" and acceptance 4 at :1063 reads "raised to 1.15". `git diff` confirms the hunk @@ -102
- [HOLDS  ] v2:602d7a2432cfd213934cfb7d976fa00b64bf3ebce54cf159993e830fd8e8ddf9: Finding CONFIRMED; not refuted. The reviewer's evidence reproduces exactly. `grep -n "1\.1[0-9]" tests/test_r_cross_implementation.py` returns exactly two sites: `255:    """`scipy.stats.chatterjeexi` (scipy >= 1.17) - independent, Python, no R needed.` and `268:        "scipy.stats", reason="scipy.stats.chatterjeexi needs scipy >= 1.15"`. `git diff tests/test_r_cross_implementation.py` shows 1 fi
- [HOLDS  ] v2:1f35d16cc9e676b4c985367f56e51ddcf3949768cdac2480414fc3cadceb795e: Could not refute; the evidence reproduces exactly and the finding is strengthened. `sed -n 1185p spec/spectests06.md` returns verbatim `| R6.8 coverage reported in CI, scipy floor raised to 1.17 | +0 | 4 | 1 |`, while the same file's acceptance clause at 1064-1067 reads "scipy's floor is raised to 1.15" and "Files: **6**". The version half is also factually wrong about shipped code, not just inter
- [HOLDS  ] v2:06fde0493bd2fa5a25365d4059a582f2101e1dacadd89724ce5b1bfebed5d470: Not refutable; the finding is confirmed and was introduced by this patch layer. Verified by running: (1) `sed -n 1185p spec/spectests06.md` reproduces `| R6.8 coverage reported in CI, scipy floor raised to 1.17 | +0 | 4 | 1 |` while acceptance 5 at :1068 says Files 6 and acceptance 4 says floor 1.15, and `pyproject.toml:31` declares `scipy>=1.15` - so the table states a floor the tree does not hav
- [HOLDS  ] v2:9f9652abfbf812d721b11ced1c17fcba0d9c5e4cb532234035acc1932cbd90f2: Not refuted - reproduced and strengthened. (1) sed -n 267,271p reproduces the quoted code verbatim. (2) pyproject.toml:31 declares "scipy>=1.15" under [project].dependencies, not optional-dependencies (groups there are only dev/r/bench), and grep -rn scipy src/quebra/ shows unconditional imports in transforms/interpolate.py, analyzers/permutation.py, analyzers/shape_stats.py and analyzers/checks/_
- [HOLDS  ] v2:554b5fb588be5f68bbafd7472abf92e39b8a6250818a6789f06f74b7f35f2f8b: Finding confirmed; I could not refute it on relevance or reachability. (1) Ground truth verified by downloading wheels, not by reading: scipy 1.14.1 does not export chatterjeexi and has no stats/_correlation.py, while 1.15.0 does both. So 1.15 is the correct floor and "scipy >= 1.17" at tests/test_r_cross_implementation.py:255 is wrong, needlessly excluding 1.15 and 1.16 under which the oracle run
- [HOLDS  ] v2:8d873fc4fe426bd6c06105a7650ff47f9d5ff83f2edefc199d1e5dbe0891635e: Could not refute; every piece of evidence reproduced. (1) git diff -- jobs/bench/arms.py confirms the re-pointed citation at lines 46-48 is part of this patch layer. (2) `grep -rn "arm_a" tests/` returns nothing (exit 1), and grepping `_tau_for|_truncate_at` across all .py files returns only definitions/uses inside jobs/bench/arms.py (defs at 204, 215; uses at 240, 244, 406, 416); the only importe
- [HOLDS  ] v2:21768fab61b826eacbc02b833e6107b8602ae2488f1dad7cc3d3c6deb2aba922: Could not refute; the finding holds and is stronger than stated. (1) The importorskip at :267 is unreachable for a stronger reason than the reviewer gave: line 32 of the same file is a module-level `from scipy.stats import rankdata, spearmanr`. I patched builtins.__import__ to raise on any scipy import and then imported tests.test_r_cross_implementation - it fails at COLLECTION time with ImportErr
- [HOLDS  ] v2:ea7f9c0e15e42be8527d1b2cd7fb638e098e28d1f72412425bae89f41f61a983: Could not refute; the finding holds and every claimed number reproduced exactly. (1) Location/quotes accurate: c2_anderson_darling.py:15-16 says "agreement is to float precision"; tests/test_checks_statistics.py:82 asserts rel=1e-9. (2) Measured relative agreement across the parametrised n (3,5,20,60,200) = 3.37e-16, 2.75e-16, 4.28e-15, 7.72e-15, 9.94e-14, matching the reviewer's figures to two si
- [HOLDS  ] v2:689ca3de04d1d2c82edf918665cb7258b39d1f36a82116daed73742f6ef51232: Could not refute; every evidence command reproduces. (1) `ls tests/ | grep -i oracle` -> no match, exit 1. (2) `git log --all --oneline -- tests/test_oracle_audit.py | wc -l` -> 0; a broader `git log --all --diff-filter=A --name-only | grep -i oracle` also returns nothing, so no such file has ever been added on any branch. (3) `grep -rn "oracle_audit" --include=*.py --include=*.md .` -> exactly on
- [HOLDS  ] v2:0fe03e0ad4d09be156f979775a4d3af65a835c6c49a39c27467601ac0a60fbd8: Not refuted; confirmed by execution. (1) The cited test does not import arms.py: exec'ing tests/test_checks_statistics.py leaves zero `jobs*` modules in sys.modules, and `grep -rn "bench.arms|bench import arms" tests/ src/` finds no import anywhere in the tree (only prose mentions). `grep -rn "arm_a" tests/` exits 1. (2) Mutation proof, run with an in-memory pytest plugin from the scratchpad (no r
- [HOLDS  ] v2:3e45d69d3f1673e4a62cbbbb2e39801eea04b1c73891bd73ecbf64b0d602158b: Could not refute; the finding holds. Verified by running: `ls tests/ | grep -i oracle` exits 1; `git log --all --oneline -- tests/test_oracle_audit.py | wc -l` is 0 and `git log --all --name-only | grep -i oracle` shows no such path in any commit; `grep -rn "oracle_audit" . --exclude-dir=.git` returns exactly one hit, tests/test_stale_references.py:5. tests/ is absent from SCOPE (line 51), so the 
- [REFUTED] v2:952f521115dc4cb0c57406c91f2183e9f8ce5d26ea686e57b6b65fc9bbb06a73: REFUTED on relevance. The reviewer's arithmetic reproduces exactly, but the defect they say could hide in the gap cannot occur in this module, and their proposed fix is unsound.

WHAT I CONFIRMED (all reproduced):
- `src/quebra/analyzers/checks/c2_anderson_darling.py:14-16` docstring cites `tests/test_checks_statistics.py::test_eq7_is_the_classical_anderson_darling`, whose tolerance is `rel=1e-9` 
- [HOLDS  ] v2:783ee5d1aefab7215dfe96cbdb25e9e2da8c462f6c86981581acbb946876f543: Could not refute; the finding fully reproduces. Applying the shipped CITATION regex and _canonical from tests/test_stale_references.py to .claude/ (recursively) yields exactly 5 stale citation sites: .claude/review-findings-0006-commit4.md:13 (tests/test_checks_c2.py), :31 (test_checks_c2.py), :32 (test_checks_c1.py), :75 (test_calendar_tau_truncation.py) - all in an UNTRACKED file - plus .claude/
- [HOLDS  ] v2:12cf249981af98cecb2ff0c4bc4849b219081dc99382aa605b70f002f76cadbd: NOT REFUTED - I reproduced the reviewer's evidence exactly and could not find any repo-specific reason the case is unreachable.

WHAT I RAN (mutants built from the shipped /home/sera/Desktop/polimi/thesis/code/912days/qre_tool/tests/test_stale_references.py with ONLY the SCOPE tuple edited, run under pytest, then deleted; working tree restored and verified with git status):
- test_mut_drop_src.py:
- [HOLDS  ] v2:8b792e91d4ec4daed02c9afb70f045869f1b7db229a026147b312dc36a165692: Not refuted - I reproduced the reviewer's evidence exactly. Mutants built from the shipped tests/test_stale_references.py with ONLY the SCOPE tuple edited (REPO_ROOT pinned to the real repo so git ls-files and is_file() still hit the actual tree, no repo edits): drop_src 2 passed, drop_jobs 2 passed, drop_docs 2 passed, drop_scripts 2 passed; only mistype_src ("srcc/") goes red (1 failed, 1 passed
- [HOLDS  ] v2:71e2ffc5957a6044f17216830f54bd6532e5131dff5c0dd83864ea134db8f7c0: Confirmed, not refuted. I validated the instrument first: applying the shipped CITATION regex and _canonical to the tracked in-scope tree reproduces the docstring's own numbers exactly (38 sites, 18 distinct, and the per-prefix breakdown src/ 13, jobs/ 14, docs/ 7, scripts/ 1, AGENTS.md 2, CONTRIBUTING.md 1). The same instrument over all of .claude/ (400 citation sites scanned) gives 5 stale sites
- [HOLDS  ] v2:d7fa394d6ca5a472669e6cf379aeba08e209cafab83d17ce801cc72ad43f4d31: Not refuted - every piece of evidence reproduced exactly. Re-running the reviewer's walk over `git ls-files` with the file's own CITATION regex gives citation sites outside SCOPE by top level of {'.claude': 387, 'CLAUDE.md': 2, 'pyproject.toml': 2, 'spec': 123, 'tests': 22}, matching the finding verbatim. SCOPE at tests/test_stale_references.py:51 is ("src/", "jobs/", "docs/", "scripts/", "AGENTS.
- [HOLDS  ] v2:c2b8f04a1eda1f8b5ab1e33ffdcd10257817fdccc25632ccef92aad18cb6a49e: NOT REFUTED - every claimed result reproduced exactly, by running it.

1. Mutation (the load-bearing half of the claim). Removing the lookbehind from `CITATION` at /home/sera/Desktop/polimi/thesis/code/912days/qre_tool/tests/test_stale_references.py:67 leaves the guard green. Run through real pytest with a plugin that rebinds the module's `CITATION` to `(?:tests/)?test_[a-z_0-9]+\.py` after collec
- [HOLDS  ] v2:da2e861d0d415466f77bcf119bb61a12cd140eb85963029f5656e92067677da3: CONFIRMED - could not refute. All three parts of the claim reproduce by measurement, and the gap is live rather than theoretical.

(1) Counts are exact. Re-running the file's own CITATION regex over `git ls-files` minus SCOPE gives {'.claude': 387, 'CLAUDE.md': 2, 'pyproject.toml': 2, 'spec': 123, 'tests': 22}, matching the reviewer digit for digit. SCOPE at tests/test_stale_references.py:48 is ("
- [HOLDS  ] v2:e5bbb4619797206c5799808c60487918ad7360eb9f0c671991c4d1e33b1c4e79: Could not refute; every claimed command reproduced. (1) `grep -rn 'defaults\|shell:' .github/workflows/` returns no match (exit 1) and the directory holds only ci.yml, so the step at ci.yml:67 runs under GitHub's Linux default `bash -e {0}` with no pipefail. (2) Copying the real Makefile into the scratchpad with scripts/fast-selector.txt absent and running `bash -e -c 'make cov | tee -a summary.tx
- [HOLDS  ] v2:d50c91700fa75c9d01ab3e80ae50c487e3dfd291a859ebd118c3e7ff7c69ec7c: Could not refute; every probe reproduces and the core defect is squarely within this repo's own rules.

CONFIRMED BY RUNNING:
(1) Mutation reproduces. A scratchpad copy of tests/test_stale_references.py with REPO_ROOT pinned to the repo and the lookbehind deleted (CITATION = r"(?:tests/)?test_[a-z_0-9]+\.py") gives "2 passed in 0.32s". An independent walk over the same tracked SCOPE yields identic
- [REFUTED] v2:3ff83a6d2f3ee6010f9adb19c33e451981b236b053b39fbc5ff57bed6c9cd71d: REFUTED on relevance. The shell mechanics are correct - I reproduced them - but the consequence the finding names cannot occur in this workflow.

Confirmed true: no `shell:`, no `defaults:`, no `if:` anywhere in .github/workflows/ (only `continue-on-error: true` at line 60), so the default `bash -e {0}` runs without pipefail. Running the exact recipe shape gave `STEP EXIT = 0` and a fenced block w
- [HOLDS  ] v2:7bab1e0df0c856b0a22406d0ae1be6f9afd694036837197a30305c4fc11e31f0: Could not refute; every element of the evidence reproduced. Ran `make cov` with data/real_private present (the condition the sentence itself states): it printed `TOTAL 6522 1961 70%`, matching the table at spec/spectests06.md:986-988 exactly and confirming the same code state, followed by `589 passed, 2 skipped, 11 warnings in 72.77s` - not the 587 written at line 999. Ran `pytest --collect-only -
- [HOLDS  ] v2:d4706cb44359d294ab9234f9af3672a7ac1a33048cd741dd02b9b848c26bb8cb: Could not refute; the finding is confirmed by direct execution. `sed -n '1041p;1185p' spec/spectests06.md` reproduces both quoted lines verbatim: 1041 is the bold normative sentence "**The `scipy` floor is raised to 1.17, and the reason in the earlier draft was wrong.**" and 1185 is the budget row "| R6.8 coverage reported in CI, scipy floor raised to 1.17 | +0 | 4 | 1 |". `grep -n scipy pyproject
- [HOLDS  ] v2:2f8d10acdab921e824c813034670cb3f0d8fb5570760c6073be6be35d0cfee23: Could not refute; every piece of evidence reproduces. (1) Location verified: .github/workflows/ci.yml lines 54-56 contain "An earlier draft copied it and had already dropped `require-selector`, which is what stops an empty selector file running every excluded tier." (2) The draft is unreachable: `git log -S\"--cov\" --oneline -- .github/workflows/ci.yml` returns nothing, `git log --all -S\"--cov=q
- [HOLDS  ] v2:fe58b76020624c84eacfdbbe1d3631e413b68ed0e2aaa40455030be4716db9ac: Finding holds; I could not refute it on relevance. Verified by running: (1) `git diff .github/workflows/ci.yml` shows the comment at :54-56 is newly added by this uncommitted patch layer, so AGENTS.md section 10 binds it - the section preamble covers "every docstring, comment, spec, ADR and doc page you write", and the "already committed instances are not a cleanup errand" carve-out does not apply
